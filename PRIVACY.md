# 巴别回声测试版：数据与录音说明

本项目目前是单机测试版。这份说明描述当前代码的数据流，供测试者在录音前判断是否适合使用；它不是针对任何地区的法律合规认证。

## 录音前

- 录音可能同时采集麦克风和电脑正在播放的声音。开始前请确认会议参与者已获适当告知，并按所在地规定和组织政策取得所需许可。
- 请确认所选语音识别与文本模型服务允许处理相应内容。不要用测试版处理受严格保密要求约束的数据。

## 数据去向与保存

- 音频片段会发送给设置中启用的语音识别服务；转写文字会按所选功能发送给文本模型，用于摘要、纪要、翻译或问答。本地模型配置只在用户自行启动相应服务后生效。各外部服务的保存期限和处理方式以其自身条款为准。
- 桌面版把会议文字、录音、配置和设置中保存的 API Key 放在当前用户的应用数据目录；源码运行时使用项目的 `data/` 和 `config.json`。当前 API Key 存储为本机明文文件，没有操作系统级加密。
- 会议会保留在本机，直到用户在历史记录中删除。删除会议会删除对应的会议 JSON 与已保存的音频；已经发送给外部模型的数据不受此操作控制。
- 上传的临时原文件和转码临时文件会在处理结束或失败后尽力删除。桌面版通过文件选择器导入时不会删除用户选中的原文件。异常退出可能留下临时文件，可在应用退出后检查本地数据目录。
- 本项目不提供账号同步或云端会议存储。把源码上传到 GitHub 不等于把本机会议数据上传；`data/`、本机配置、密钥和构建产物均被 Git 忽略。

## 使用边界

默认 HTTP 服务仅供本机使用，且没有用户账号认证。请勿将其暴露在局域网或公网；当前版本会拒绝非本机 Host 和跨来源浏览器请求。共享电脑上的其他本地进程仍可能访问本机服务。请按设备本身的账户与磁盘保护要求管理数据。

如需用于正式业务或受监管资料，应先独立评估录音告知、供应商协议、数据保留、访问控制、加密与删除要求。
